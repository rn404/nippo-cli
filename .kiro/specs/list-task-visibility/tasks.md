# Implementation Plan

- [x] 1. ステータス別分類ロジック（`SplitByStatus`）の実装とユニットテスト
  - `internal/log` に、ログのアイテムをclosedタスク・open/startedタスク・メモの3グループに分類する関数を実装する
  - 各グループを作成日時の昇順で整列する（既存の `Split` と同じソート方式を踏襲）
  - closed/open/started/メモが混在するケース、全アイテムがclosedのケース、空のログのケースをそれぞれ検証するユニットテストを作成する
  - Observable: `go test ./internal/log/...` で新規テストが全てパスし、3つの戻り値の合計要素数が入力アイテム数と一致することが確認できる
  - _Requirements: 1.1, 3.2, 4.1, 4.2_
  - _Boundary: log.SplitByStatus_

- [ ] 2. `list` コマンドの可視性ロジックとCLI統合
- [x] 2.1 デフォルト非表示・`--full`・`--task` の可視性ロジックを実装
  - `list` コマンドのオプションにcompleted表示切り替え用のフラグ値（`Full`）とタスク限定表示用のフラグ値（`TasksOnly`）を追加する
  - 日次表示（stat無し）で、タスク1の分類関数を使ってclosedタスク・open/startedタスク・メモを組み立て、`Full` が指定されないときはclosedタスクを、`TasksOnly` が指定されたときはメモを表示対象から除く
  - 既存のタグフィルタは、可視性の絞り込みを行ったあとの結果に対して適用し、グルーピング順序を保つ
  - stat表示（`-s`）と全ファイル一覧（`-a`、stat無し）の処理では新しいフラグ値を一切参照せず、既存の出力をそのまま維持する
  - Observable: 完了済みタスクのみのログに対しフラグ無しで日次表示を実行すると、既存の「アイテムなし」メッセージが表示される
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 3.1, 3.2, 3.3, 5.1_
  - _Boundary: command.listOneDay_

- [ ] 2.2 `--full`/`--task` フラグのCLI登録
  - `list` コマンドに `--full`（完了済みタスクを含める）と `--task`（メモを除外する）のフラグを登録する
  - 登録したフラグの値を2.1で追加したオプションに橋渡しする
  - Observable: `sava list --full` および `sava list --task` を実行するとエラーなく受理され、それぞれ対応するオプション値が設定された状態でコマンド処理に渡る
  - _Requirements: 2.1, 3.1_
  - _Boundary: cmd.newListCommand_
  - 2.1で追加したオプションのフィールドに依存するため、2.1の完了が前提（直前タスクのため暗黙の依存として扱う）

- [ ] 3. 統合テストとリグレッション確認
- [ ] 3.1 可視性・グルーピング挙動の統合テスト
  - フラグ無しでの日次表示がclosedタスクを除外し、open/startedタスクとメモを含むことを検証する
  - 対象日の全アイテムがclosedタスクのとき、`--full` 無しでは「アイテムなし」メッセージになることを検証する
  - `--full` 指定時にclosedタスクを含む全件が「closed→open/started→メモ」の順で出力されることを検証する
  - `--task` 指定時（`--full` 有無それぞれ）にメモが除外され、`--full` 無しではclosedタスクも除外されることを検証する
  - 各ステータスグループ内でアイテムが作成日時昇順に並ぶことを検証する
  - グループの区切りが見出しの追加ではなく、アイテムの並び順のみで表現されていることを検証する（出力に区切り用の見出し文字列が含まれないことを確認）
  - Observable: `go test ./internal/command/...` で上記シナリオを検証する新規テストが全てパスする
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3_
  - _Boundary: command.listOneDay_

- [ ] 3.2 既存フィルタ・他モードとの非干渉のリグレッションテスト
  - `--tag` フィルタを `--full`/`--task` や既定の可視性ルールと組み合わせたとき、指定条件をすべて満たすアイテムのみが表示され、グルーピング順序が保たれることを検証する
  - 既存の `-s`（stat）テストと `-a`（全ファイル一覧、stat無し）テストに `--full`/`--task` を付与したケースを追加し、出力が変化しない（新フラグが無視される）ことを検証する
  - Observable: `go test ./internal/command/...` の既存 `TestListStatAndAll` 系テストに追加したケースを含め全て変わらずパスする
  - _Requirements: 5.1, 5.2, 5.3_
  - _Boundary: command.listOneDay, command.List_
  - 3.1と同じテストファイルを編集するため3.1の完了後に着手する（直前タスクのため暗黙の依存として扱う）
