# Implementation Plan

- [x] 1. 中断通知関数（view層・command層）の実装とユニットテスト
  - `internal/view`に、`EditAborted`と同型の固定メッセージ出力関数を2つ追加する（メモ追加が中断されたことを示す文言、タスク追加が中断されたことを示す文言）
  - `internal/command`に、上記2関数へ薄く委譲するだけの関数を2つ追加する（`EditAborted`の委譲パターンを踏襲）
  - それぞれの出力内容を検証するユニットテストを`internal/view`に追加する
  - `internal/command`の委譲関数が対応する`view`関数を正しく呼び出すことを検証するテストを追加する
  - Observable: `go test ./internal/view/... ./internal/command/...`と`golangci-lint run`がいずれもパスし、新規追加した4関数それぞれの単体テストが存在する
  - _Requirements: 2.3, 2.4_
  - _Boundary: view.AddAborted, view.TodoAborted, command.AddAborted, command.TodoAborted_

- [ ] 2. `sava add`のcontent省略時エディタ分岐の実装とエンドツーエンドテスト
  - `cmd/sava`に、content引数の有無に応じてcontentを解決するヘルパーを実装する：引数が指定されていればそのまま使い、省略されていれば既存の`editor.Resolve`を空の初期内容で呼び出し、保存内容が空文字列または空白文字のみであれば「中断」と判定する
  - `add`コマンドの引数指定を必須から任意（0個または1個）に変更し、上記ヘルパーの判定結果に応じて、メモを作成するか、1で実装した中断通知を表示するかを分岐する
  - `$EDITOR`に一時的なシェルスクリプトを設定し、実際に内容を書き込んで保存した場合にその内容でメモが作成されることを検証するエンドツーエンドテストを書く
  - エディタで何も書かずに保存した場合、および空白文字のみを書いて保存した場合の両方で、メモが作成されず中断メッセージが表示されることを検証するエンドツーエンドテストを書く
  - エディタスクリプトが非ゼロ終了する場合にエラーとして終了し、メモが作成されないことを検証するエンドツーエンドテストを書く
  - `--tag`付きでエディタ経由でメモを作成した場合にタグが付与されること、および中断・エラー時にはタグ付け処理が実行されないことを検証するエンドツーエンドテストを書く
  - content引数を直接指定した場合にエディタが起動されず、従来通り即座に処理されることを検証する回帰テストを書く（空文字列直接指定時の拒否は`empty-content-validation`スペックの既存テストで既にカバーされているため、本タスクでは新規テストを追加しない）
  - Observable: `go test ./cmd/sava/...`と`golangci-lint run`がパスし、`sava add`（引数省略）がエディタ経由でメモを作成・中断・エラーの各ケースで期待通りに動作することが実際のバイナリ経由のテストで確認できる
  - _Requirements: 1.1, 1.3, 1.4, 1.5, 2.1, 2.3, 2.4, 2.5, 3.1, 3.4, 4.1, 4.2_
  - _Boundary: cmd.resolveNewContent, cmd.newAddCommand_
  - _Depends: 1_

- [ ] 3. `sava todo`のcontent省略時エディタ分岐の実装とエンドツーエンドテスト
  - 2で実装したcontent解決ヘルパーを`todo`コマンドでも再利用し、引数指定を任意（0個または1個）に変更する
  - ヘルパーの判定結果に応じて、タスクを作成するか、1で実装した中断通知を表示するかを分岐する
  - `$EDITOR`に一時的なシェルスクリプトを設定し、実際に内容を書き込んで保存した場合にその内容でタスクが作成されることを検証するエンドツーエンドテストを書く
  - エディタで何も書かずに保存した場合、および空白文字のみを書いて保存した場合の両方で、タスクが作成されず中断メッセージが表示されることを検証するエンドツーエンドテストを書く
  - エディタスクリプトが非ゼロ終了する場合にエラーとして終了し、タスクが作成されないことを検証するエンドツーエンドテストを書く
  - `--start`付きでエディタ経由でタスクを作成した場合に着手状態になること、`--tag`付きの場合にタグが付与されること、および中断・エラー時にはどちらの処理も実行されないことを検証するエンドツーエンドテストを書く
  - content引数を直接指定した場合にエディタが起動されず、従来通り即座に処理されることを検証する回帰テストを書く（空文字列直接指定時の拒否は`empty-content-validation`スペックの既存テストで既にカバーされているため、本タスクでは新規テストを追加しない）
  - Observable: `go test ./...`と`golangci-lint run`がリポジトリ全体でパスし、`sava todo`（引数省略）がエディタ経由でタスクを作成・中断・エラーの各ケースで期待通りに動作し、`--start`/`--tag`が直接指定モードと同様に効くことが実際のバイナリ経由のテストで確認できる
  - _Requirements: 1.2, 1.3, 1.4, 1.5, 2.2, 2.3, 2.4, 2.5, 3.2, 3.3, 3.4, 4.1, 4.2_
  - _Boundary: cmd.newTodoCommand_
  - _Depends: 2_
