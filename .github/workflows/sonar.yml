name: Comment Sonar

on:
  workflow_dispatch:

permissions:
  pull-requests: write

jobs:
  sonar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run comment-sonar
        uses: rn404/comment-sonar@31f2da00edfef64697e0605df72d67cf2bf1ab85
